---
title: 基础内容
layout: page
order: 1
---
***

<h1></h1>

{% assign child_pages = site.pages | where: "parent", "基础内容" | sort: "order" %}

{% for child in child_pages %}
  <section id="{{ child.title | slugify }}">
    <h2>{{ child.title }}</h2>
    {{ child.content | markdownify }}
  </section>
{% endfor %}
